<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Submissions</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <link rel="stylesheet" href="/styles/collector.css">
    <link rel="icon" href="/images/EcoPoint.png" type="image/x-icon">
</head>

<body>
    <%- include('../partials/nav1') %>

    <div class="container mt-5">
        <h2 class="text-center contributor-name">Points Accumulated for <%= collector.name %></h2>

        <% if (message) { %>
            <div class="alert alert-warning text-center message-alert"><%= message %></div>
        <% } %>

        <% if (submissions.length > 0) { %>
            <table class="table table-bordered mt-4 submissions-table">
                <thead>
                    <tr>
                        <th>Manufacturer Name</th>
                        <th>Total Points</th>
                    </tr>
                </thead>
                <tbody>
                    <% submissions.forEach(submission => { %>
                        <tr>
                            <td><%= submission.manufacturerName %></td>
                            <td><%= submission.totalPoints %></td>
                        </tr>
                    <% }) %>
                </tbody>
            </table>
        <% } %>
    </div>

    <%- include('../partials/footer') %>
</body>

</html>